# AI Sentinel - Frontend (React / Vite)

This folder contains the converted React + Vite frontend for the TailGuard project.

Quick start (frontend):

1. cd frontend
2. npm install
3. npm run dev

The frontend connects to the backend at `http://127.0.0.1:8000` by default.
